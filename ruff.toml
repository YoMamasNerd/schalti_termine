line-length = 110
target-version = "py312"

[lint]
# E501 bewusst NICHT aktiv: ~70 Langzeiler in Hand-Code (staff_views, smtp,
# fsm_sync, forms). Erst umbrechen, dann aktivieren. Migrations sowieso nie.
select = ["E4", "E7", "E9", "F", "I", "W"]

[lint.per-file-ignores]
# Auto-generierte Migrations enthalten lange Zeilen (Field-Defaults, choices)
"termine/migrations/**" = ["E501"]